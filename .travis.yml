sudo: required

env:
  global:
    - TAG_BRANCH=${TRAVIS_BRANCH/\//--}
  matrix:
   - REPO=pravindahal/simple-php-project.web
   - REPO=pravindahal/simple-php-project.nginx

language: bash

services:
  - docker

script:
  - docker pull $REPO
  - docker build -f docker/web/Dockerfile -t $REPO:$TRAVIS_COMMIT .
  - docker tag $REPO:$TRAVIS_COMMIT $REPO:$TAG_BRANCH
  - docker tag $REPO:$TRAVIS_COMMIT $REPO:latest
  - docker login -u $DOCKER_USER -p $DOCKER_PASS
  - docker push $REPO

notifications:
  slack: futurehome:uOTCFkT2d7kTcrymCTUFaEcv
